<!---いいね10_like_buttonsにrenderさせる--->
<%= form_with url: post_likes_path(post), method: :post, data: { turbo_stream: true }, html: { id: "tenth-like-button-#{post.id}" } do %>
  <%= hidden_field_tag :like_type, 'like_tenth' %>
  <button type="submit" class="flex items-center space-x-1 text-gray-400 px-4 py-2">
    <i class="fa-solid fa-hands-clapping text-blue-950"></i>
    <p class="text-xs" id="like-count-like_tenth-<%= post.id %>">
      <%= @likes_by_post[post.id]&.count { |l| l.like_type == 'like_tenth' } || 0 %>
    </p>
  </button>
<% end %>