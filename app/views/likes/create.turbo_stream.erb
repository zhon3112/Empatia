<!-- いいね1 -->
<%= turbo_stream.replace "first-like-button-#{@post.id}" do %>
  <%= render 'posts/first_unlike', post: @post %>
<% end %>
<%= turbo_stream.update "like-count-like_first-#{@post.id}" do %>
  <%= @post.likes.where(like_type: 'like_first').count %>
<% end %>

<!-- いいね2 -->
<%= turbo_stream.replace "second-like-button-#{@post.id}" do %>
  <%= render 'posts/second_unlike', post: @post %>
<% end %>
<%= turbo_stream.update "like-count-like_second-#{@post.id}" do %>
  <%= @post.likes.where(like_type: 'like_second').count %>
<% end %>
