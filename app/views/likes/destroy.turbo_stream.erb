<!--- いいね1 --->
<%= turbo_stream.replace 'first-like-button' do %>
  <%= render 'posts/first_like', post: @post %>
<% end %>

<%= turbo_stream.update 'like-count' do %>
  <p class="text-xs"><%= @post.likes.where(like_type: 'like_first').count %></p>
<% end %>
